# MicLink服务器环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# ========== 服务器配置 ==========
# 服务器监听端口
SERVER_PORT=8080

# ========== 安全配置 ==========
# API密钥 - 用于客户端认证
# 重要: 生产环境必须修改此密钥！
# 建议使用强随机字符串，长度至少32位
API_KEY=miclink-default-key-change-in-production

# ========== IP白名单配置（可选）==========
# 是否启用IP白名单检查
# true: 仅允许白名单中的IP访问
# false: 仅检查API密钥，不限制IP
ENABLE_IP_WHITELIST=false

# 允许访问的IP地址列表（逗号分隔）
# 仅在ENABLE_IP_WHITELIST=true时生效
# 示例: ALLOWED_IPS=192.168.1.100,192.168.1.101,10.0.0.5
ALLOWED_IPS=

# ========== 使用说明 ==========
# 1. 复制此文件为 .env
# 2. 修改API_KEY为随机生成的强密钥
# 3. 如需启用IP白名单，设置ENABLE_IP_WHITELIST=true并配置ALLOWED_IPS
# 4. 确保客户端Config.kt中的API_KEY与此处匹配

# ========== 密钥生成建议 ==========
# 在Linux/Mac上可以使用以下命令生成随机密钥:
# openssl rand -base64 32
#
# 在Windows PowerShell上可以使用:
# -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
